{{ if .Values.glooingress.enabled }}
apiVersion: gateway.solo.io/v1
kind: RouteTable
metadata:
  name: statemachine2-transactional-kafka
  namespace: {{ .Release.Namespace }}
  labels:
    namespace: {{ .Release.Namespace }}
    app: statemachine2-transactional-kafka
spec:
  routes:
  - matchers:
    - methods:
      - GET
      - POST
      - DELETE
      regex: /v1/statemachine2-transactional-kafka
    routeAction:
      single:
        upstream:
          name: statemachine2
    routeAction:
      single:
        upstream:
          name: statemachine2
{{- end }}